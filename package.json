{
  "devDependencies": {
    "concurrently": "^8.2.1",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    "dev": "cd ./docs && npx concurrently \"npx tailwindcss -c ../tailwind.config.js -i ./css/input.css -o ./css/output.css --watch\" \"npx live-server --no-css-inject\""
  }
}
